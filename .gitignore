# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets — never commit these
.env
.env.local
# The filled-in copy of deploy/secret.example.yaml. Both spellings, because
# `*.secret.yaml` alone does NOT match a file simply named secret.yaml.
deploy/secret.yaml
*.secret.yaml

# Node / frontend
node_modules/
frontend/dist/
frontend/probe-dist/
frontend/.vite/

# Editors / OS
.DS_Store
.idea/
.vscode/
