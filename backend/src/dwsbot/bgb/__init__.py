"""Black Gold Battlefield: the roster we register, and the cards we hand out.

`sheet` is the two-team spreadsheet an admin fills in from the registration
screenshots; `cards` turns a recorded roster into the briefing images;
`lineup` is the renderer those images come out of.
"""
